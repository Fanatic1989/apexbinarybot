# ─────────────────────────────────────────
# Deriv Signal Bot — Python Dependencies
# Requires Python 3.11 (set in render.yaml)
# All packages have pre-built wheels — no
# compilation required, fast install.
# ─────────────────────────────────────────

# Web server
flask==3.0.3
gunicorn==21.2.0

# Deriv WebSocket connection
websocket-client==1.7.0

# Data processing (pre-built wheels on 3.11)
pandas==2.1.4
numpy==1.26.4

# Technical indicators
ta==0.11.0

# HTTP requests (Telegram API + scraper)
requests==2.31.0

# HTML parsing (ForexFactory scraper)
beautifulsoup4==4.12.3

# Environment variables
python-dotenv==1.0.0
